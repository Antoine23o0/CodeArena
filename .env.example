# Copy this file to `.env` to configure docker compose bindings for your environment.
# These defaults target local development (frontend reachable on all interfaces while backend
# services stay private on the Docker network). Override the values below when deploying on the VM
# 195.15.242.237 so every container advertises the correct public URL automatically.

# Public host information shared by the stack (used to compute API/CORS/web URLs).
PUBLIC_HOST=localhost
PUBLIC_WEB_ORIGIN=http://localhost:5173
PUBLIC_API_URL=/api
# Leave PUBLIC_SOCKET_URL empty to reuse the same origin for Socket.IO. Override it only when
# the websocket endpoint is exposed elsewhere.
PUBLIC_SOCKET_URL=

# Ports and bind addresses for each service. On the VM set PUBLIC_HOST to 195.15.242.237 and
# expose only the frontend (web) service; the API and MongoDB communicate exclusively through
# the internal Docker network.
WEB_BIND_HOST=0.0.0.0
WEB_PORT=5173
API_PORT=3000
MONGO_PORT=27017
MONGO_EXPRESS_BIND_HOST=127.0.0.1
MONGO_EXPRESS_PORT=8081
