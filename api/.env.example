# MongoDB connection string.
# For local development without Docker, install MongoDB locally and use:
# MONGO_URI=mongodb://localhost:27017/codearena
# When running through docker compose, the bundled MongoDB uses credentials root/rootpassword.
# Update the URI accordingly if you connect to that instance from the host machine.
MONGO_URI=mongodb://localhost:27017/codearena

# Secret key used to sign JWT tokens. Replace with a strong secret in production.
JWT_SECRET=change-me

# Comma-separated list of origins allowed to access the API via CORS.
# Include the VM frontend origin when deploying to 195.15.242.237.
ALLOWED_ORIGINS=http://localhost:5173,http://195.15.242.237:5173

# Absolute or relative path to the Python runner used to evaluate submissions.
RUNNER_PATH=../runner/run.py

# Optional port override for the Express server (defaults to 3000).
# PORT=3000

# Automatically peupler la base avec les 10 concours d'exemple au démarrage de l'API.
# Laissez la valeur à "false" si vous préférez gérer le contenu manuellement.
AUTO_SEED_CONTESTS=true
