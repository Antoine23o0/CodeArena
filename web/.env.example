# Base URL for the REST API (without trailing slash).
# Replace `localhost` by the public IP of the VM when deploying (e.g. http://195.15.242.237:3000/api).
VITE_API_URL=http://localhost:3000/api

# Base URL for the Socket.IO server used for live scoreboards.
# Replace `localhost` by the public IP of the VM when deploying (e.g. http://195.15.242.237:3000).
VITE_SOCKET_URL=http://localhost:3000
